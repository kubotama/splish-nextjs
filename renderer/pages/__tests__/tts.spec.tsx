import { render, screen } from "@testing-library/react";

describe("変換ブロック", () => {
  it.todo(
    "登録ボタンがクリックされて登録されたテキストに設定されると、変換ボタンが有効になる"
  );
  it.todo(
    "変換ボタンがクリックされると、変換されたテキストに設定されて、再生ボタンが有効になる"
  );
});
